# Прокси: /api -> backend (FastAPI), / -> miniapp (статика Vite-сборки)
events {}

http {
  server {
    listen 80;

    location /api/ {
      proxy_pass http://backend:8000/;
    }

    location / {
      proxy_pass http://miniapp:5173/;
    }
  }
}
